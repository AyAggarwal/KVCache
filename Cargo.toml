[package]
name = "kvcache"
version = "0.0.1"
edition = "2024"

[profile.release]
debug = 1

[features]
default = []
multi_threaded = []

[dependencies]
mio = { version = "1.0.2", features = ["os-poll", "net"] }
redis-protocol = "6.0.0"
